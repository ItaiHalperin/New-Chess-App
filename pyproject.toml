[tool.black]
line-length = 120
include = '\.pyi?$'
exclude = '''
(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | venv
    | .venv
    | _build
    | buck-out
    | build
    | dist
  )/
)
'''

[tool.mypy]
exclude = ['venv', '.venv','eyq']

